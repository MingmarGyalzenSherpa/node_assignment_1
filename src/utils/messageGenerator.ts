export const notFound = (title: string) => `${title} not found`;

export const found = (title: string) => `${title} found successfully`;
export const created = (title: string) => `${title} created successfully`;

export const updated = (title: string) => `${title} updated successfully`;

export const deleted = (title: string) => `${title} deleted successfully`;
