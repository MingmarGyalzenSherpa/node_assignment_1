export const notFound = (title: string): string => `${title} not found`;

export const fetched = (title: string): string =>
  `${title} fetched successfully`;

export const successful = (title: string): string => `${title} successful`;

export const created = (title: string): string =>
  `${title} created successfully`;

export const alreadyExists = (title: string): string =>
  `${title} already exists`;

export const updated = (title: string): string =>
  `${title} updated successfully`;

export const deleted = (title: string): string =>
  `${title} deleted successfully`;

export const invalid = (title: string): string => `Invalid ${title}`;
